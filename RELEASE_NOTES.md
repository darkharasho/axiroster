# Release Notes

Version v0.1.0 — initial

First tagged build of AxiRoster. Future versions' notes are generated by
`npm run release` (see docs/RELEASING.md); this file always holds the notes for
the version currently being released.
