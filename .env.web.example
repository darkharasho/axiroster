# Web build (Vite) — copy to .env and fill with your Supabase project values.
# These are the SAME public values the desktop app uses (anon key is safe in a
# browser client; RLS enforces access).
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY
